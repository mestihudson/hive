const { Given, When, Then } = require('@cucumber/cucumber')

Given(`eu sou um visitante`, () => {
  return `pending`
})

When(`eu me registro`, () => {
  return `pending`
})

Then(`eu sou um usuário`, () => {
  return `pending`
})
